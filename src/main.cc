#include <map>

#include "./Map/s21_map.tpp"
using namespace std;
using namespace s21;
int main() {
  map<int, int> myMap;
  std::cout << myMap.size() << std::endl;
  // Set<int> mySet;
  // Set<int> mySet1;
  // std::cout << mySet.size() << std::endl;

  // for (auto i : mySet) {
  //   std::cout << i << std::endl;
  // }
}
